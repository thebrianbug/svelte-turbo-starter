# Database Configuration
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/svelte_turbo_db

# Environment (development, test, production)
NODE_ENV=development

# Note: When NODE_ENV=test, the database name will automatically be changed to svelte_turbo_test_db
# unless a specific test database is already included in DATABASE_URL
